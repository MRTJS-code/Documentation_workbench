CREATE TABLE [ods].[DIM_GT_ACTIVITY] (
    [oid]         VARCHAR (34) NOT NULL,
    [code]        VARCHAR (50) NOT NULL,
    [description] VARCHAR (50) NOT NULL,
    PRIMARY KEY CLUSTERED ([oid] ASC)
);

